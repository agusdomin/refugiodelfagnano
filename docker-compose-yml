version: "3.9"  # optional since v1.27.0
services:
  app:
    image: refugio-fagnano-react-dev-v1.1
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./src:/app/src
    
